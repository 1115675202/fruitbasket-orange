spring:
  devtools:
    restart:
      enabled: false #热部署是否生效
  jpa:
    show-sql: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/zhaoyun?${custom.datasource.url-param}
    username: root
    password: 1pTVg0ld@1909

springfox:
  documentation:
    enabled: false
    swagger-ui:
      enabled: ${springfox.documentation.enabled}
